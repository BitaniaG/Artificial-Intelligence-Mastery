medical_warehouse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      user: postgres
      password: postgres
      port: 5433
      dbname: medical_warehouse
      schema: public
      threads: 4